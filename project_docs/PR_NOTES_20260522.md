# TradingCoach PR Notes 草稿 — 2026-05-22

> 本文用于当前 `fix/dogfood-bugs` 分支 PR 描述。事实来源：`REMAINING_TASKS_20260521.md`、`REMAINING_ACCEPTANCE_20260521.md`、浏览器 QA 与自动化门禁输出。

## 1. 改动摘要

- 修复统计接口与前端展示的可信度问题：by-grade 支持 `C+?` / `C?` / `C-?`，Sharpe 口径统一，Backtest 百分比空值不再触发崩溃。
- Dashboard 接入待复盘交易，Positions/Statistics/Detail 统一展示不完整评分说明。
- 修复多币种展示：Recent/Related/Detail 等持仓级金额按 position currency 渲染，HKD 不再显示成 USD。
- 修复实际上传后进入的 `/analysis/:taskId` 取消流程：pending/running 可取消，成功/失败 toast 清楚，completed/failed/cancelled 终态不显示取消按钮。
- 修复 Landing 恢复 banner 轮询同值状态时的重复 setState，避免 `Maximum update depth exceeded`。
- 修复移动端页面级横向溢出，保留表格内部横向滚动。
- 清理前端 lint/type 问题，补充格式化工具单测。

## 2. 浏览器 QA

已用 Playwright 覆盖以下页面和流程，均无 `console.error` / `pageerror`：

- Landing/upload：恢复 banner、文件选择、email 输入、通知开关、执行按钮。
- Position detail：Summary、Risk、Execution、News/Event、Related positions、评分 tooltip，无 `NaN` / `undefined`。
- AI Coach：规则洞察 fallback 正常；未配置 LLM key 时明确显示不可用并禁用 chat input。
- 移动端 390px：Dashboard、Statistics、Positions、Backtest 无页面级横向溢出。
- 双语：中文 Dashboard 待复盘/评分，英文 Backtest 文案可读。
- Positions `score_grade=C?` 筛选：可见行均为 `C?`，未混入 `C+?` / `C-?`。
- 多币种：`/positions/460` 顶部和明细金额显示 `HK$`。
- 取消 UI：成功、失败、终态不可取消三条分支均验证。

## 3. 自动化验证

- `cd frontend && npm run typecheck`：通过。
- `cd frontend && npm run lint && npm run test:unit && npm run build`：通过，unit 9 passed。
- `/opt/homebrew/bin/python3.11 -m pytest tests/unit/test_symbol_matcher.py tests/unit/test_fifo_matcher.py tests/data_integrity ...`：业务单元与数据完整性路径 93 passed；集成测试需 `factory-boy`，裸 Homebrew Python 环境缺依赖。
- `/opt/homebrew/bin/uv run --python /opt/homebrew/bin/python3.11 --with-requirements requirements.txt python -m pytest tests/integration/test_api_statistics.py tests/integration/test_api_positions.py`：40 passed。
- 后端取消流程：临时 SQLite DB 中创建真实 pending task，`DELETE /api/v1/tasks/{task_id}` 返回 success，状态变为 `cancelled`。
- `git diff --check`：通过。
- 本地提交已按主题拆分：`cb7b3fe` 后端指标、`05ca355` 前端收口流程、`0c3ba9e` 前端 lint/图表清理，另有文档收口提交。

## 4. 安全和隐私

- secrets 扫描未发现真实密钥。命中项均为示例变量名、配置读取、GitHub Actions `${{ secrets.GITHUB_TOKEN }}`、文档说明、或 package lock 中依赖名 `js-tokens`。
- 本轮未新增真实券商 CSV / xlsx / 截图 / 账户信息。
- 新增文档不包含邮箱、账户、券商导出原文或 API key。

## 5. Deferred

- 评分系统本身仍大量输出 `?`，当前 PR 只让 UI/筛选/统计诚实展示不完整评分；是否调整 `_check_input_completeness()` 阈值需要单独数据研究。
- 聚合统计的跨币种口径仍建议后续统一为 USD 等价或返回 currency breakdown；本 PR 已先修复持仓级别误导展示。
- Chrome/Playwright QA 已覆盖关键路径，但还未做完整视觉回归和 a11y 专项审计。
- PR 发布尚未执行，需要用户明确批准后才能 push / open PR；当前文档仍是 PR body 草稿。
